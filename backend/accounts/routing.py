from django.urls import re_path

# Liste des modèles de routage WebSocket
websocket_urlpatterns = [
    # Ajoutez ici vos modèles d'URL WebSocket
    # Exemple : re_path(r'ws/some_path/$', consumers.YourConsumer.as_asgi()),
]
